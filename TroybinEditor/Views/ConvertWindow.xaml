<Window x:Class="TroybinEditor.Views.ConvertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Troybin ↔ Text Converter" Height="600" Width="720"
        MinHeight="480" MinWidth="560"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        ShowInTaskbar="True">
    <Window.Resources>
        <Style x:Key="ModeButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource SurfaceLightBrush}" BorderThickness="0,0,0,1" Padding="20,14">
            <StackPanel Orientation="Horizontal">
                <Border Width="32" Height="32" Background="{StaticResource PrimaryBrush}"
                        CornerRadius="8" Margin="0,0,12,0">
                    <TextBlock Text="🔄" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Troybin ↔ Text Converter" FontSize="15" FontWeight="Bold"
                               Foreground="{StaticResource TextBrush}"/>
                    <TextBlock Text="Convert .troybin files to editable text and back"
                               FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Mode selector -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource SurfaceLightBrush}" BorderThickness="0,0,0,1" Padding="20,12">
            <StackPanel>
                <TextBlock Text="CONVERSION MODE" FontSize="10" FontWeight="Bold"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="RadioToText" Content="📤  .troybin → .txt" GroupName="Mode"
                                 Style="{StaticResource ModeButtonStyle}"
                                 IsChecked="True" Checked="Mode_Changed" Margin="0,0,8,0"/>
                    <RadioButton x:Name="RadioToTroybin" Content="📥  .txt → .troybin" GroupName="Mode"
                                 Style="{StaticResource ModeButtonStyle}"
                                 Checked="Mode_Changed"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- File list -->
        <Grid Grid.Row="2" Margin="20,16,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,8">
                <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                           FontSize="10" FontWeight="Bold"
                           Foreground="{StaticResource TextSecondaryBrush}">
                    <Run x:Name="FileListLabel" Text="INPUT FILES (.troybin)"/>
                </TextBlock>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="AddFilesBtn" Content="➕ Add Files" Click="AddFiles_Click"
                            Style="{StaticResource ModernButtonStyle}" Padding="12,5" Margin="0,0,6,0"
                            FontSize="11"/>
                    <Button x:Name="AddFolderBtn" Content="📁 Add Folder" Click="AddFolder_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="{StaticResource SurfaceLightBrush}"
                            Padding="12,5" Margin="0,0,6,0" FontSize="11"/>
                    <Button Content="🗑 Clear" Click="ClearFiles_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="{StaticResource SurfaceLightBrush}"
                            Padding="12,5" FontSize="11"/>
                </StackPanel>
            </DockPanel>

            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource SurfaceLightBrush}" BorderThickness="1" CornerRadius="6">
                <ListBox x:Name="FileListBox"
                         Background="Transparent" BorderThickness="0"
                         Padding="4" FontFamily="Consolas" FontSize="11"
                         Foreground="{StaticResource TextBrush}"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         AllowDrop="True" Drop="FileList_Drop" DragOver="FileList_DragOver">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="Icon" Text="📄 " FontSize="11"/>
                                <TextBlock Text="{Binding}" FontSize="11" Foreground="{StaticResource TextBrush}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Output folder selector -->
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Output folder:" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource SurfaceLightBrush}" BorderThickness="1"
                        CornerRadius="4" Padding="10,7">
                    <TextBlock x:Name="OutputFolderLabel" Text="(same as input file)"
                               FontFamily="Consolas" FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis"/>
                </Border>
                <Button Grid.Column="2" Content="Browse…" Click="BrowseOutput_Click"
                        Style="{StaticResource ModernButtonStyle}"
                        Background="{StaticResource SurfaceLightBrush}"
                        Padding="12,7" Margin="8,0,0,0" FontSize="11"/>
            </Grid>
        </Grid>

        <!-- Log output -->
        <Border Grid.Row="3" Margin="20,12,20,0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource SurfaceLightBrush}" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="LOG" FontSize="10" FontWeight="Bold"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="10,8,0,0"/>
                <ScrollViewer Grid.Row="1" x:Name="LogScroller"
                              VerticalScrollBarVisibility="Auto" Padding="10,4">
                    <TextBlock x:Name="LogBox" FontFamily="Consolas" FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextWrapping="Wrap"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Bottom buttons -->
        <Border Grid.Row="4" Padding="20,12"
                BorderBrush="{StaticResource SurfaceLightBrush}" BorderThickness="0,1,0,0"
                Background="{StaticResource SurfaceBrush}" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" x:Name="StatusLabel"
                           Text="Add files and click Convert"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="11" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Close" Click="Close_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="{StaticResource SurfaceLightBrush}"
                            Padding="16,8" Margin="0,0,8,0"/>
                    <Button x:Name="ConvertBtn" Content="🔄  Convert" Click="Convert_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Padding="20,8" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>

